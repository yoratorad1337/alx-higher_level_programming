#!/usr/bin/python3

def element_at(my_list, idx):
    if (-idx and idx > len(mylist)):
        return NONE
    else:
    x = print("Element at index " + my_list[:] + "is " + my_list[idx])
    return my_list[x]
